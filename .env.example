# =============================================================================
# Crew Performance Chatbot - Local / RDS Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Never commit .env to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Database (RDS or local PostgreSQL)
# -----------------------------------------------------------------------------
DB_HOST=oe-data.cn4yiea887u8.ap-south-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=z7j9Wn0dwUG?P]AijzHg?B4FgAc$
DB_SSL=true

# For local PostgreSQL, use:
# DB_HOST=localhost
# DB_SSL=false

# -----------------------------------------------------------------------------
# Claude API (required for chat and AI features)
# -----------------------------------------------------------------------------
ANTHROPIC_API_KEY=sk-ant-api03-...
CLAUDE_MODEL=claude-3-5-sonnet-20241022

# -----------------------------------------------------------------------------
# Application
# -----------------------------------------------------------------------------
NODE_ENV=development
API_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Frontend - API base URL (used by Vite in the browser)
# Set this to your backend API URL after the Amplify sandbox prints it.
# -----------------------------------------------------------------------------
VITE_API_URL=http://localhost:20002

# -----------------------------------------------------------------------------
# Feature flags and rate limiting (optional)
# -----------------------------------------------------------------------------
ENABLE_SUMMARY_GENERATION=true
ENABLE_RISK_DETECTION=true
SUMMARY_REFRESH_DAYS=15
MAX_TOKENS_PER_REQUEST=4000
MAX_REQUESTS_PER_MINUTE=20
